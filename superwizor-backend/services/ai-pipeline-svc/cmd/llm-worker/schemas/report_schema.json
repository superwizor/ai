{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "maxLength": 100,
      "description": "Krótki, opisowy tytuł raportu."
    },
    "summary_short": {
      "type": "string",
      "maxLength": 500,
      "description": "Streszczenie sesji — 2-3 zdania."
    },
    "speaker_role_inference": {
      "type": "object",
      "description": "Wynik diaryzacji wykonanej przez LLM (zob. ADR-IMPL-007).",
      "properties": {
        "method": {
          "type": "string",
          "enum": ["llm_inferred", "native_chirp_3"],
          "description": "llm_inferred = ta sesja ma diaryzację przez LLM (default w v1.2). native_chirp_3 = STT już zrobił diaryzację (przyszły flow)."
        },
        "speaker_groups": {
          "type": "array",
          "description": "Lista zdedukowanych mówców. Każdy chunk powinien należeć do dokładnie jednej grupy (lub do osobnej grupy 'filler' dla krótkich wtrąceń).",
          "items": {
            "type": "object",
            "properties": {
              "role": {
                "type": "string",
                "enum": ["therapist", "patient", "couple_partner", "family_member_parent", "family_member_child", "family_member_sibling", "third_party"]
              },
              "chunk_indices": {
                "type": "array",
                "items": {"type": "integer"},
                "description": "Lista indeksów chunków przypisanych do tego mówcy."
              },
              "confidence": {
                "type": "number",
                "description": "0-1, jak pewna jest dedukcja roli."
              },
              "evidence": {
                "type": "string",
                "maxLength": 200,
                "description": "Krótkie uzasadnienie z transkryptu."
              }
            },
            "required": ["role", "chunk_indices", "confidence", "evidence"]
          }
        },
        "overall_diarization_confidence": {
          "type": "number",
          "description": "0-1, ogólna pewność co do całej diaryzacji."
        }
      },
      "required": ["method", "speaker_groups", "overall_diarization_confidence"]
    },
    "main_themes": {
      "type": "array",
      "maxItems": 6,
      "items": {
        "type": "object",
        "properties": {
          "theme": {"type": "string", "maxLength": 200},
          "salience": {"type": "number", "description": "0-1 jak ważny był ten temat"},
          "evidence_quotes": {
            "type": "array",
            "maxItems": 3,
            "items": {"type": "string", "maxLength": 200},
            "description": "Krótkie cytaty z transkryptu."
          }
        },
        "required": ["theme", "salience", "evidence_quotes"]
      }
    },
    "therapeutic_alliance_observations": {
      "type": "string",
      "maxLength": 800,
      "description": "Obserwacje dotyczące przymierza terapeutycznego. UWAGA: low-confidence w MVP gdy method=llm_inferred."
    },
    "interventions_observed": {
      "type": "array",
      "maxItems": 8,
      "items": {
        "type": "object",
        "properties": {
          "intervention_type": {"type": "string", "maxLength": 80},
          "description": {"type": "string", "maxLength": 200},
          "patient_response": {"type": "string", "maxLength": 200}
        },
        "required": ["intervention_type", "description", "patient_response"]
      }
    },
    "hitop_dimensions": {
      "type": "array",
      "maxItems": 6,
      "items": {
        "type": "object",
        "properties": {
          "dimension_code": {
            "type": "string",
            "enum": ["INTERNALIZING", "EXTERNALIZING", "THOUGHT_DISORDER", "DETACHMENT", "DISTRESS", "FEAR", "DISTRESS_DEPRESSION", "DISTRESS_ANXIETY", "FEAR_PANIC", "FEAR_SOCIAL", "ANTAGONISM", "DISINHIBITION"],
            "description": "Kod wymiaru HiTOP (mierzonego DLA pacjenta — używaj chunków przypisanych do roli 'patient')"
          },
          "score": {"type": "number", "description": "0-100"},
          "confidence": {"type": "number", "description": "0-1"},
          "evidence": {"type": "string", "maxLength": 200, "description": "Krótkie uzasadnienie."}
        },
        "required": ["dimension_code", "score", "confidence", "evidence"]
      }
    },
    "risk_assessment": {
      "type": "object",
      "properties": {
        "level": {"type": "string", "enum": ["low", "medium", "high"]},
        "concerns": {
          "type": "array",
          "maxItems": 5,
          "items": {"type": "string", "maxLength": 200}
        },
        "recommended_actions": {
          "type": "array",
          "maxItems": 5,
          "items": {"type": "string", "maxLength": 200}
        }
      },
      "required": ["level", "concerns"]
    },
    "sentiment": {
      "type": "string",
      "enum": ["positive", "neutral", "concerning"]
    },
    "recommendations_for_next_session": {
      "type": "array",
      "maxItems": 5,
      "items": {"type": "string", "maxLength": 200},
      "description": "3-5 punktów, każdy pojedyncze zdanie."
    },
    "rag_summary_chunk": {
      "type": "string",
      "maxLength": 1500,
      "description": "Krótkie streszczenie kluczowych informacji do zapisu w RAG memory. NIE zawierać danych identyfikujących."
    }
  },
  "required": [
    "title", "summary_short", "speaker_role_inference", "main_themes",
    "hitop_dimensions", "risk_assessment", "sentiment",
    "rag_summary_chunk"
  ]
}
