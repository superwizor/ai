// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: payment_events.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createPaymentEventStub = `-- name: CreatePaymentEventStub :one
INSERT INTO payment_events (
    provider, provider_event_id, event_type,
    raw_payload, processing_status, error_message,
    received_at
) VALUES (
    $1, $2, $3, $4, 'IGNORED', $5, now()
)
RETURNING id, provider, provider_event_id, event_type, processing_status, received_at
`

type CreatePaymentEventStubParams struct {
	Provider        PaymentProvider `json:"provider"`
	ProviderEventID string          `json:"provider_event_id"`
	EventType       string          `json:"event_type"`
	RawPayload      []byte          `json:"raw_payload"`
	ErrorMessage    *string         `json:"error_message"`
}

type CreatePaymentEventStubRow struct {
	ID               uuid.UUID       `json:"id"`
	Provider         PaymentProvider `json:"provider"`
	ProviderEventID  string          `json:"provider_event_id"`
	EventType        string          `json:"event_type"`
	ProcessingStatus string          `json:"processing_status"`
	ReceivedAt       time.Time       `json:"received_at"`
}

// Insert webhook event jako IGNORED (stub mode). UNIQUE constraint na
// (provider, provider_event_id) zwróci unique-violation przy duplikacie —
// caller (StripeStubHandler) używa tego do idempotency check.
func (q *Queries) CreatePaymentEventStub(ctx context.Context, arg CreatePaymentEventStubParams) (CreatePaymentEventStubRow, error) {
	row := q.db.QueryRow(ctx, createPaymentEventStub,
		arg.Provider,
		arg.ProviderEventID,
		arg.EventType,
		arg.RawPayload,
		arg.ErrorMessage,
	)
	var i CreatePaymentEventStubRow
	err := row.Scan(
		&i.ID,
		&i.Provider,
		&i.ProviderEventID,
		&i.EventType,
		&i.ProcessingStatus,
		&i.ReceivedAt,
	)
	return i, err
}
