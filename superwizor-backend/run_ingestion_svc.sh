#!/bin/bash
export GCP_PROJECT_ID="superwizor-ai-25ecd"
export AUDIO_BUCKET_NAME="superwizor-ai-25ecd-audio-uploads"
# Find database password or use Cloud SQL Auth Proxy?
# But wait, the DB might not be exposed publicly.
